# ===================================
# JRM CRM - Environment Configuration
# ===================================
# Kopiera denna fil till .env och fyll i dina värden
# VIKTIGT: Lägg ALDRIG till .env i Git!

# -----------------------------------
# Application Settings
# -----------------------------------
NODE_ENV=production
PORT=8080

# -----------------------------------
# Cosmos DB Configuration
# -----------------------------------
# Hämta från: Azure Portal → Cosmos DB → Keys
COSMOS_ENDPOINT=https://jrm-crm-cosmos.documents.azure.com:443/
COSMOS_KEY=your-cosmos-primary-key-here
COSMOS_DATABASE=jrm-crm-db

# -----------------------------------
# Azure AD B2C Authentication
# -----------------------------------
# Hämta från: Azure Portal → Azure AD B2C → App registrations
AZURE_AD_B2C_TENANT_NAME=your-tenant-name
AZURE_AD_B2C_CLIENT_ID=your-client-id-here
AZURE_AD_B2C_CLIENT_SECRET=your-client-secret-here
AZURE_AD_B2C_POLICY_NAME=B2C_1_signupsignin
AZURE_AD_B2C_REDIRECT_URI=https://jrm-crm-api.azurewebsites.net/auth/callback

# För lokal utveckling:
# AZURE_AD_B2C_REDIRECT_URI=http://localhost:3000/auth/callback

# -----------------------------------
# Session Configuration
# -----------------------------------
# Generera med: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
SESSION_SECRET=your-secure-random-string-min-32-chars

# -----------------------------------
# Application Insights (Monitoring)
# -----------------------------------
# Hämta från: Azure Portal → Application Insights → Properties
APPINSIGHTS_INSTRUMENTATIONKEY=your-appinsights-key-here

# -----------------------------------
# CORS Configuration
# -----------------------------------
# Frontend URL som får anropa API:et
CORS_ORIGIN=https://jrm-crm-frontend.azurewebsites.net

# För lokal utveckling:
# CORS_ORIGIN=http://localhost:8080

# Flera origins (separera med komma):
# CORS_ORIGIN=https://app1.com,https://app2.com

# -----------------------------------
# Visma.net Integration (Optional)
# -----------------------------------
# Endast om du vill använda Visma.net integration
# VISMA_CLIENT_ID=your-visma-client-id
# VISMA_CLIENT_SECRET=your-visma-client-secret
# VISMA_TENANT_ID=your-visma-tenant-id
# VISMA_REDIRECT_URI=https://jrm-crm-api.azurewebsites.net/api/visma/callback

# -----------------------------------
# Email Service (Optional - för framtida funktioner)
# -----------------------------------
# SENDGRID_API_KEY=your-sendgrid-api-key
# EMAIL_FROM=noreply@jrm-crm.com

# -----------------------------------
# SMS Service (Optional - för framtida funktioner)
# -----------------------------------
# TWILIO_ACCOUNT_SID=your-twilio-account-sid
# TWILIO_AUTH_TOKEN=your-twilio-auth-token
# TWILIO_PHONE_NUMBER=+46701234567

# -----------------------------------
# Rate Limiting
# -----------------------------------
# Max antal requests per IP per 15 minuter
RATE_LIMIT_MAX=100
RATE_LIMIT_WINDOW_MS=900000

# -----------------------------------
# Logging
# -----------------------------------
LOG_LEVEL=info
# Nivåer: error, warn, info, verbose, debug

# -----------------------------------
# Feature Flags (Optional)
# -----------------------------------
# Aktivera/inaktivera funktioner
FEATURE_EXPORT_ENABLED=true
FEATURE_BATCH_OPS_ENABLED=true
FEATURE_GLOBAL_SEARCH_ENABLED=true
FEATURE_VISMA_SYNC_ENABLED=false

# -----------------------------------
# Development Settings (Endast lokal utveckling)
# -----------------------------------
# Sätt till true för att köra utan databas (mock data)
# USE_MOCK_DATA=true

# Debug mode
# DEBUG=cosmos:*,azure:*
