services:
  web:
    image: jrm-crm:latest
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:3000"
    environment:
      - PORT=3000
      - ADMIN_PASSWORD=ChangeThisStrong!
      - DATA_DIR=/app/data
    volumes:
      - ./docker-data:/app/data
      - ./Sammanställning mäklardata och kunder 1.xlsx:/app/Sammanställning mäklardata och kunder 1.xlsx:ro
      - ./Editerad version.xlsx:/app/Editerad version.xlsx:ro
      - ./Import.xlsx:/app/Import.xlsx:ro
    restart: unless-stopped
